function setCSS_proj() {
    /* var stored = sessionStorage.getItem("stylePref");
    var oldlink = document.getElementsByTagName("link").item(0);
    var newlink = document.createElement("link");

    if (stored === "CSS/styleDark.css" || stored === "CSS/styleLite.css") {
        var newStored = "../../" + stored;
        console.log(newStored);
    } else  if (stored === "../../CSS/styleDark.css" || stored === "../../CSS/styleLite.css") {
        var newStored = stored;
        console.log(newStored);
    } else {
        console.log("nothing stored");
    }

    sessionStorage.setItem("stylePref", newStored);

    newlink.setAttribute("rel", "stylesheet");
    newlink.setAttribute("type", "text/css");
    newlink.setAttribute("href", newStored);

    document.getElementsByTagName("head").item(0).replaceChild(newlink, oldlink);

    if (newStored === "../../CSS/styleDark.css" || newStored === "CSS/styleDark.css"){
        document.getElementById("mode-toggle").checked = true;
        changeCSS_proj("../../CSS/styleLite.css");
    }
    else {
        document.getElementById("mode-toggle").checked = false;
        changeCSS_proj("../../CSS/styleDark.css");
    }
*/
}